{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "-- Grafana --"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [
        {"expr": "sum by (service) (rate(linkedin_service_http_requests_total[5m]))", "refId": "A"}
      ],
      "title": "Requests / sec by service",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "-- Grafana --"},
      "fieldConfig": {"defaults": {"unit": "percent"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {"expr": "linkedin_service_cache_hit_rate_percent", "refId": "A"}
      ],
      "title": "Cache hit rate %",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "-- Grafana --"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
      "id": 3,
      "targets": [
        {"expr": "histogram_quantile(0.95, sum by (le, service) (rate(linkedin_service_http_request_duration_seconds_bucket[5m])))", "refId": "A"}
      ],
      "title": "p95 latency by service",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["linkedin-clone", "ops"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "LinkedIn Clone Service Overview",
  "version": 1
}
