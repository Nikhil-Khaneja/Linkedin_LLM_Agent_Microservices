# Copy to .env.aws.mysql (gitignored) and adjust if your RDS/password differs.
# Start the SSH tunnel first (see infra/ecs-ec2/README.md), then:
#   set -a && source .env.aws.mysql && set +a && ./scripts/bulk_seed_aws_mysql.sh
#
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=linkedin_sim
