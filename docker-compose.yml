version: "3.9"

services:
  mysql:
    image: mysql:8
    container_name: owner1-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_access
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7
    container_name: owner1-redis
    ports:
      - "6379:6379"

volumes:
  mysql_data: